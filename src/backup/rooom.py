# Implement a class to hold room information. This should have name and
# description attributes.


class Room():
    def __init__(self, name, desc, key, list):
        self.name = name
        self.desc = desc
        self.key = key
        self.list = list
